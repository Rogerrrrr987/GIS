# GeoCanvas 面試展示指南

## 專案定位

GeoCanvas 是不依賴 Python 或後端資料庫的瀏覽器 GIS 工作站。Windows 使用者可透過 PowerShell 本機伺服器啟動，完成向量繪製、格式轉換、屬性編輯、空間處理、多點路網分析及工作復原。

## 五分鐘展示流程

1. 雙擊 `run_gis.bat`，說明專案可在一般 Windows 環境直接啟動。
2. 匯入一份 KML、GeoJSON、CSV 或 Shapefile ZIP，展示圖層與屬性資料表。
3. 使用緩衝區、裁切或相交工具，說明運算在瀏覽器本機完成且不修改原始圖層。
4. 開啟路網分析，展示點位排序、唯一起終點、屏障與 OSRM 傳輸同意機制。
5. 儲存 `.gcp.json` 專案，重新整理後展示自動備份與復原。
6. 雙擊 `run_tests.bat`，展示路網、安全、空間處理與多語系測試。

## 可討論的工程設計

- 純前端模組化架構，避免本機安裝 Python 或資料庫。
- SHP、KML/KMZ、GeoJSON、CSV/WKT 的匯入與匯出管線。
- 使用安全 AST 解析器實作欄位計算器，不使用 `eval` 或 `new Function`。
- 多點路網分析支援 200 點、2-Opt 本機最佳化、分段 OSRM 請求及屏障繞行。
- 外部座標傳輸採 fail-closed 同意流程；TGOS APIKey 不進入專案檔或永久備份。
- Undo/Redo、自動備份、未保存提醒及專案復原等資料保護機制。
- 自動化測試涵蓋路網、安全、空間運算與國際化功能。

## 已知限制

- 前端函式庫與底圖仍需網路連線載入。
- OSRM 與 TGOS 屬外部服務，功能可用性受服務狀態及使用配額影響。
- 瀏覽器儲存空間有限，重要成果應另存 `.gcp.json` 或 GIS 交換格式。
- 此版本以 Windows 本機使用為主要交付情境，不是多人協作後端系統。

## 驗證指令

```powershell
.\run_tests.ps1
```

目前四套測試共包含 84 個測試案例；提交版本前應確認全部通過。
